训练代码能够运行，不代表模型已经具有可靠的通用能力。
验证损失可以帮助发现过拟合和数据管线错误。
分词器、模型结构与 checkpoint 的词表大小必须保持一致。
缓存生成时，每一层都保存历史 token 的键和值。
流式输出改善等待体验，但不会改变模型本身的准确率。
较长上下文会增加注意力计算和 KV Cache 内存。
如果损失突然变成非数，应检查学习率、数据和混合精度。
监督微调的数据应当只对助手答案计算主要损失。
偏好训练之前需要一个已经会基本对话的参考模型。
模型评测应保留失败例子，而不是只展示成功回答。
在笔记本上训练小模型时，先跑小配置能节省大量排错时间。
所有公开数据都应核对许可证、来源和允许用途。
用户问到未知事实时，可靠的回答会明确表达不确定性。
两加四等于六，三乘三等于九。
海水通常含有盐分，纯水在常温下接近无色透明。
太阳为地球提供光和热，但太阳不是围绕地球运行的。
一个优秀仓库应当包含文档、配置、测试和可重复命令。
训练与推理使用相同的对话模板，可以减少格式分布偏移。
服务上线后要检查健康接口、普通响应和流式响应。
模型越小，越需要对任务范围作诚实说明。
